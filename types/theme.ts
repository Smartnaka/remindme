/**
 * Color theme interface for type-safe theme usage throughout the app
 */
export interface ColorTheme {
  primary: string;
  primaryLight: string;
  textDark: string;
  textMuted: string;
  cardBackground: string;
  background: string;
  success: string;
  error: string;
  warning: string;
  tint: string;
}
